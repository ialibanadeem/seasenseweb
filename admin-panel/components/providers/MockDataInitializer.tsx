'use client';

export function MockDataInitializer() {
    // Purposely gutted to remove all mock data. 
    // The Dashboard now waits passively for real hardware WebSockets to broadcast telemetry events.
    return null;
}
